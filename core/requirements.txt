fastapi==0.139.2        # pinned == (not floor): test_api.py's route-walker
                        # reads a FastAPI-internal structure, so CI-vs-local
                        # version drift is a real fragility risk (mirrors the
                        # exact ruff/black pins)
starlette==1.3.1        # pinned with fastapi — the ASGI layer under it
python-multipart==0.0.32  # required for multipart/form-data (résumé/JD uploads); pinned ==
uvicorn[standard]>=0.30.0
flask>=3.0.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
asyncpg>=0.29.0         # raw asyncpg + hand-written SQL — no ORM, no Alembic
neo4j>=5.24.0
arq>=0.26.0
redis>=5.0.1           # Redis.aclose() (worker/main.py shutdown) landed in 5.0.1
httpx>=0.27.0           # the OpenAI-compatible LLM client is hand-rolled on httpx
tenacity>=9.0.0
python-dotenv>=1.0.0
jinja2>=3.1.0           # prompt templates (src/prompts)
pyyaml>=6.0.0           # skill alias table (src/pipeline/skill_data/aliases.yaml)
rapidfuzz==3.10.1       # stage-3 anti-fabrication verifier (pinned == for gate
                        # reproducibility; partial_ratio is the load-bearing metric)

# ── Document extraction ──────────────────────────────────────────────────────
PyMuPDF>=1.24.0         # import fitz
python-docx>=1.1.0      # import docx
striprtf>=0.0.26        # lazy-imported by the RTF branch only
